/**
 * Declare module's config object here
 * The config object is used as a center point of all environment variables
 * Ex:
 * export const configs = {
 *  variable: process.env.NEXT_APP_VARIABLE || "value"
 * }
 */
